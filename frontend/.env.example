# Leave empty when using Vite locally or the Docker/Nginx reverse proxy.
VITE_API_BASE_URL=

# Optional: backend origin used by Vite's /api proxy during development.
VITE_API_PROXY_TARGET=http://localhost:8080

# GitHub Pages build path; leave / for local development.
VITE_BASE_PATH=/
